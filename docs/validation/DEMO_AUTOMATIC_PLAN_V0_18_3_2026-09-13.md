# 0.18.3 自动计划演示本机验收

日期：2026-09-13  
环境：Windows，项目 `.venv`

## 自动化结果

执行：

```powershell
.\.venv\Scripts\python.exe -m pytest -q
```

结果：`1644 passed, 4 subtests passed in 118.77s`。

专项覆盖：

- 四项演示评估具有独立 SYNTHETIC / TEST 来源，并由正式自动规则生成计划。
- 次数上限、投影角目标、计划来源和不可修改元数据符合现有自动计划契约。
- 连续生成会归档旧演示计划，只保留一个活动版本；评估指纹和参与者指纹可在重开后复核。
- 训练中心按钮可以切换到演示用户和演示来源，并打开自动计划窗口。
- 合成来源隐藏录像选择、禁用打开预览，不能伪装成真实摄像头训练。

## 本机数据确认

通过应用存储接口在 `data/home_rehab.sqlite3` 中创建了演示用户及一套活动计划：左肩外展、左肩前屈、左肘屈伸、右膝伸展，均为 5 次 × 1 组；投影目标分别为 63.2°、73.6°、103.0°、41.0°。本机数据库按 `.gitignore` 保留，不上传仓库。

## 边界

本验收证明自动计划的软件链路和数据隔离，不证明合成角度符合真实患者状况，也不构成临床有效性、处方安全性或真人动作识别准确度验证。
