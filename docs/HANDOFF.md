# 当前交接与未完成目标

更新：2026-09-12。当前应用 **0.18.0 自动康复闭环版**。有效评估可自动汇总为身体测试情况，生成每轮最多 4 项的基础活动计划，并按顺序准备训练、监督已设置目标和记录训练后感受；现有 53 项动作和左右侧全部接入。公共动作来源与软件评估适配明确分开，疾病 / 术后处方仍不自动生成。当前电脑通过 **1641 项主测试和 4 项子测试**、新增自动闭环定向 154 项测试及 5 个相关原生界面检查；改动说明见[自动评估到训练闭环](history/AUTOMATIC_REHAB_V0_18.md)，实际证据见[0.18.0 本机验收](validation/AUTOMATIC_REHAB_V0_18_2026-09-12.md)。

0.18 的自动计划只使用同用户、来源和情境下 7 天内的最新自动评估；要求有效比例至少 0.8 且至少观察到 1 次完整动作。每项先 1 组、最多 5 次且不超过评估完成次数；角度是本人投影范围内的工程适配，不是正常值。开始训练时重新检查评估指纹、规则版本、个人条件、计划参数和执行顺序。档案已有疾病 / 活动限制、本人报告不适或明显疲劳时停止自动续练。实际范围与来源见版本说明。

0.17 在 `061c0a6`（0.16.0）上改的是流程与呈现，不是测量：摆好位置自动进入下一步；条件一致时沿用上次起点 / 坐站基线并说明；跟踪编号跳动不再要求重新核对，只有画面里出现第二人或一度无人超过 2 秒才撤销；同一次准备里的反复缺测需持续更久才再提示一次；开始前有可取消的 3 秒倒计时；保存后停留在本次任务看完成小结和下一步；日常活动 / 卧室 / 安全演示也有同款步骤卡。**引导计时练习**（`guided-prompt-1`）在不能自动测量时按固定节奏提示动作，可手动记次和暂停提示，记录标注 `measurement_mode='guided_timed'`，不作为训练所需的评估依据。未新增模型、依赖、动作或示范素材，未放宽任何测量阈值。

0.16.0 由同学提供的 `health-care-software-main.7z` 接入，保留 3 个新增提交及双方合并历史；当时本机复核为 1450 项主测试和 4 项子测试。来源、目录和当时证据见[整合验收](validation/INTEGRATION_2026-09-12.md)。

0.16 在 `f52ae5b` 的 0.15 融合版上新增“银发健康守护”：活动任务与持久化决策、固定参考变化卡、自报共享、本机家庭回应 / 整改、隔离手机演示，见[功能与算法说明](history/SILVER_HEALTH_V0_16.md)。可选服务独立队列，不改主姿态模型、临床库结构或双摄主测量。手机仅 SYNTHETIC / TEST 资料，不连接患者库；当前依然不是全天候远程照护产品。

原 0.15 融合同学分支 `199a265` 的逐步流程 / 结果解读与 GitHub `f7c69bb` 的提示缓冲 / 倒计时采样 / 双摄逐项有效性。准备按步骤推进，起点、方向和坐站记录使用可取消倒计时，最后一次按钮确认；普通和大字指导继续共用提示策略。包内交接记载“用户反馈双摄可用”，本次未重新打开硬件验证，软件测试不代替真人精度或硬件长期验收。

同学的 0.9.1 已于 `7bb7019` 完成整合；随后已接入个人计划库、逐动作时间、纵向历史和正面 / 侧面双摄的软件流程。此前双摄开发阶段仅枚举一台设备的历史限制见[双摄版本说明](history/DUAL_CAMERA_V0_13.md)，不要当作用户当前设备状态。此前续建状态见[执行计划](plans/PRODUCT_CONTINUATION_2026-09-10.md)。

此前融合实际结果见[0.15.0 融合验收](validation/MERGE_V0_15_2026-09-12.md)。旧分支的勾选确认和双摄全项门槛不再代表当前流程；最终核对通过明确按钮完成，辅助几何缺测与主测量分别判定。

整合版曾通过 1102 项主测试和 4 项子测试，并修复 Windows 中文路径下可选 MediaPipe 模型加载；见[整合验收](validation/INTEGRATION_2026-09-10.md)。新增计划库的实际结果见[0.10.0 功能与验收](history/TRAINING_PLAN_LIBRARY_V0_10.md)，不要把不同阶段测试数量相加。

## 本次压缩包与当前版本的关系

本次 7z 的 `main` 为 `dea7aa1`（0.16.0），源码与提交一致，无未提交改动或未解决冲突。它包含本机原 `f7c69bb`（0.14.0），新增 `199a265`、双父合并 `f52ae5b` 和 `dea7aa1`，可以直接快进接入。9 月 10 日的 12 个提交属于上次 zip 导入，已在既有历史中；本次没有重复导入或重写它们。

仍未完成的是文档中的长期产品目标，不是需要猜测补齐的未提交代码。压缩包没有提供持续任务的运行状态，不能据此确认同学电脑上是否还有任务在跑。历史计划保留在 [产品持续计划](plans/PRODUCT_COMPLETION_PLAN.md) 和 [关节扩展计划](plans/JOINT_EXPANSION_PLAN.md)，本页给出当前可执行的交接状态。

## 已有功能

| 内容 | 当前工程实现 |
| --- | --- |
| 主界面 | 身体部位导航、动作搜索、评估、训练中心、身体档案、历史记录 |
| 摄像头 | 全程序共用设备选择；康复可选正面＋侧面双摄；点击后才开启；独立原始画面测试；默认镜像显示 |
| 动作 | 9 个部位、53 项任务；先选动作，再按对应测量定义观察，不自动识别任意动作名称 |
| 评估 | 单项评估与多项评估清单、舒适起点 / 方向记录、逐项可见性、保存与重开 |
| 训练 | 可持久化的个人多项计划、版本 / 归档管理、逐项复用；本次人工确认、组间休息、暂停 / 恢复、结束反馈、大字指导 |
| 记录 | 本地个人档案、评估与来源计划快照、SQLite v4 备份迁移、报告导出、保存失败恢复 |
| 配套场景 | 活动管理、卧室和低位安全事件的单独演示；三者共用同款步骤卡 |
| 顺畅流程 | 自动摆位确认、准备复用、按证据撤销核对、安静提示升级、可取消倒计时、完成小结与下一步 |
| 继续完成 | 引导计时练习（固定节奏提示）、手动记一次、暂停提示；报告分列自动观察 / 近似 / 自报 / 未取得 |
| 银发增量 | 活动许可 / 目标、延期 / 拒绝 / 核实、固定个人参考、感受共享、本机家属回执、人工整改；隔离测试手机演示 |

53 项包括不同起点与方向的任务，不等于 53 个独立解剖关节，也不等于已完成 53 项真人准确度验收。头颈和躯干项目是实验性整体投影观察。

| 本地组件 | 任务数 | 依赖 |
| --- | ---: | --- |
| YOLO / COCO17 | 19 | 主 `.venv` 与 `yolo11n-pose.pt` |
| 腕部组合关键点 | 4 | `.venv-landmarks` 与 Pose / Hand 模型 |
| 踝部 Pose 关键点 | 2 | `.venv-landmarks` 与 Pose 模型 |
| 手指 Hand 关键点 | 28 | `.venv-landmarks` 与 Hand 模型 |

同一时刻仍为一个活动场景。康复任务可以选择两个本地相机观察同一人；主机位按动作定义测量，辅助机位独立二维观察。其他场景和录像仍为单路。所有摄像头均归同一个 CameraManager 拥有，可选关键点进程使用主机位图像。

## 仍未完成 / 尚未验证

| 项目 | 当前状态 | 继续需要的条件 |
| --- | --- | --- |
| 引导计时的真实可用性 | 只做了合成输入的软件验收；固定 3 / 4 / 4 秒节奏是否适合真实使用者没有证据 | 真实使用者按提示完成一次并反馈节奏；必要时再讨论是否允许人工调整节奏 |
| 真人康复动作能否稳定识别 | 没有完成独立、可复核的真人对照验收；同学记录了现场问题并修过代码，不能将修复记录当成验收通过 | 获准使用的真人录像或现场演示；明确机位、动作、侧别与人工计数 / 标注；逐层检查骨架、几何、计数、保存 |
| 示范图片 / 视频 | 文字、阶段联动和缺图显示已有；实际动作示范素材未补齐 | 与侧别、起点和阶段对应的素材、使用权与内容核对；[素材接入要求](../rehab_codex_single_camera_v2_1/assets/exercise-guides/README.md) |
| 双摄实际效果 / 空间融合 | 包内文档记载用户 2026-09-11 反馈两路使用无问题；本次仅做软件复核，未新增硬件或真人准确度证据；曝光同步与三维融合未实现 | 进一步记录同时采集、实际参数、断流和释放重开；空间融合另需标定与同步证据 |
| 部分精细方向 | 肩 / 髋旋转、肩水平运动、前臂旋转、拇指对掌及 CMC、足趾链等未实现；最新阶段已暂缓继续扩展 | 独立测量定义、足够关键点与拍摄条件、失效门禁、人工对照；[覆盖说明](history/FUNCTIONAL_EXPANSION_V0_5.md) |
| 产品最终验收 | 个人计划库、组次执行、逐动作时间及纵向历史已通过软件回归；动作素材和真实使用验证未完成 | 核对动作内容与获准样本、人工对照、现场操作和报告重开的证据 |
| 现场可靠性 / 可用性 | USB 插拔、真实相机长期运行、远距离阅读与不同使用者的操作表现未完成系统验收 | 对应硬件、现场使用者、连续运行和异常操作记录 |

## 当前继续推进

0.16 的已接入范围见功能说明，不代表整份远程健康计划全部落地。包内引用的 `E:/SILVER_HEALTH_INCREMENTAL_PLAN.md` 原始增量文件不在本次压缩包中，本次没有据此猜测补做新功能。后续依次验证：陌生使用者可用性 → 真实双摄活动 / 计时对照 → 实际手机前台与本机声音 → 获授权真实数据的 HTTPS / 账号 / 远程部署。通用日程与静默时段、家属代报、环境照片、选做健康事项和临床重复性仍未做。严格参考条件可能使跨次起点不同的记录不能比较，不能为显示趋势悄悄放宽。

个人计划库、[逐动作时间记录](history/MOVEMENT_TIMING_V0_11.md)及[纵向历史](history/LONGITUDINAL_HISTORY_V0_12.md)已完成软件验收；[动作内容与真实验收入口](validation/ACCEPTANCE_READINESS_2026-09-10.md)也已完成盘点并提供登记模板和检查工具。本轮续建范围完成，真人、素材与硬件的最终验收仍需对应条件。

后续接入摄像头时，可按[双摄说明](history/DUAL_CAMERA_V0_13.md)先测试两路原始画面。开发工具 `check_dual_camera.py` 可记录显式选择的设备同时采集、实际帧参数及停止重开；不会推理或保存视频，不能代替真人动作对照。

优先完成一项真人肩外展的完整路径：独立相机测试 → 选择动作与测试侧 → 正式预览和确认 → 对照动作识别与计数 → 保存 → 关闭重开报告。接着验证坐站，再按所需场景选取扩展动作。每次只对已取得的证据作结论。

真人环节同时核对本轮流程改动：摆好位置是否真的自动进入下一步、重开预览沿用起点的提示是否准确、缺测提示是否不再打断、引导计时能否让人顺利做完并保存。这些目前只有合成输入的证据。

如先使用录像，需要记录来源和人工对照，保留 `REPLAY_FILE` 标签；软件合成测试继续标记 `SYNTHETIC/TEST`。目标完成与否应以可复核结果判断，不能用按钮数量、示范图或测试总数代替。

## 数据与历史

- 当前电脑原有 `data/` 保留，整理前已备份；同学压缩包中的个人数据库没有混入当前用户记录。
- 本次 7z、SHA256、整理前与同学历史 bundle、隔离源码和本机数据备份位于 `backups/incoming/2026-09-12/`，不上传；早期备份继续保留。
- 9 月 10 日的 12 个提交和本次的 3 个新增提交均已保留；本次复核以[9 月 12 日整合验收](validation/INTEGRATION_2026-09-12.md)为准，历史结果不充作新结果。
