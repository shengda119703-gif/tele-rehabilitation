# 应用开发约束

2026-09-13 自动计划演示授权：0.18.3 在训练中心提供“一键生成演示计划”，只向固定演示用户写入明确标记的 SYNTHETIC / TEST 合成评估，随后调用正式 `general-activity-rules-1` 生成并持久化临时自动计划。合成值不是正常范围、临床结论或真人测量；界面不得允许该来源打开假摄像头或执行真人训练。重复生成只归档该演示用户之前的自动计划，不修改其他用户、来源或情境数据。详见[0.18.3 说明](../docs/history/DEMO_AUTOMATIC_PLAN_V0_18_3.md)。

2026-09-13 顺畅启动授权（覆盖下文旧的多人、缺人、关节可见和强制校准门禁）：用户要求康复任务不再因人数、当前姿态、必要关节暂不可见、辅助机位缺人或未完成手工起点 / 方向采样而拒绝确认、开始或组间继续。0.18.2 使用 `nonblocking-observation-1`：输入已打开即可按流程确认和开始；无效片段仍为缺测，不计角度、次数或有效时长，恢复清楚后自动继续。基线动作在首段有效画面建立本轮起点，方向型动作由第一次明确活动自动确定画面方向；坐站以首个有效坐位建立本轮相对阈值。每路自动选择面积较大且靠近中心的主要参与者，并按已有轨迹 / 空间邻近连续跟踪；陪同者入镜不撤销确认、不停止任务，且不得把这种空间聚焦称为人脸或生物身份识别。肩动作只需肩、肘参与主角度，头颈前屈 / 后伸只需同侧眼、耳参与主角度；髋、膝、坐站和躯干等实际几何仍在运行中缺点时记为缺测，但不能作为启动门槛。相机未打开、输入结构损坏、保存失败、训练计划 / 评估来源不成立及明确的陪同安全安排仍属于真实业务或完整性条件，不得用伪数据绕过。详见[0.18.2 说明](../docs/history/NONBLOCKING_START_V0_18_2.md)。

2026-09-13 下一步提示授权：用户要求动作提示表示“接下来要做什么”，不能只是复述摄像头当前识别状态。0.18.1 将观察阶段与用户指令分离：`WAIT_READY` 仍提示保持准备；确认到 `REST` / `SEATED_READY` 后提示动作出程；确认站位、回程或已达到目标证据后提示回位；完整回位后重新提示下一次出程。短暂缺测沿用最后一条已确认指令但隐藏阶段图，不根据无效帧推进。纠错、安全、暂停、休息、完成及保存失败继续高于动作提示；引导计时仍按固定时钟提示，不能称为自动判断。不得为让提示提前而降低计次、目标、稳定时间或关键点门槛。详见[0.18.1 说明](../docs/history/NEXT_ACTION_GUIDANCE_V0_18_1.md)。

2026-09-12 自动闭环授权：用户要求评估后无需再逐项填写动作、次数和组数，由软件汇总本人测试结果、生成基础活动计划并按顺序监督完成。0.18.0 的 `general-activity-rules-1` 已接入现有 53 项动作和左右侧；只使用同用户 / 来源 / 情境、7 天内、有效比例至少 0.8 且有完整动作的最新自动评估，引导计时不作为依据，最新失败不回退旧成功。每轮最多 4 项、1 组且次数不超过本次评估已观察次数，角度只取本次投影范围内的保守目标；无直接公共动作条目的项目明确标为“本人评估适配”，不得把 WHO 的一般活动原则写成具体处方证据。疾病、术后、档案限制、不适、明显疲劳、协助与站立支撑仍需本人简短确认；不默认勾选。自动计划保存规则、评估、来源和适用性快照，开始时重建核对，不允许从人工计划入口修改或越序。实时监督只汇总已设目标和可见指标，不输出整体动作合格率、诊断或肌力结论。详见[0.18 说明](../docs/history/AUTOMATIC_REHAB_V0_18.md)。

2026-09-12 顺畅流程授权：用户要求整套应用像居家运动 App 一样顺利完成，可用性优先于精确测量。0.17.0 据此减少重复确认与重复校准、让普通提示不打断，并新增引导计时继续路径。明确变更三处既有契约：准备复用与核对失效改按可复核条件判定（`source_ref` / `source_kind` / `usage_context` / `frame_size` / `schema_id` / `model_manifest_id` / `exercise_id` / `side` / `view` / `participant_id`），`track_id` 与 `preview_epoch` 仍记录但不参与判定；核对改为按证据撤销（出现第二人立即撤销，画面里一度无人持续 2 秒撤销）；保存成功后停留在本次任务而不是跳转身体档案。采样对短暂多人 / 无人分别容忍 0.8 / 1.5 秒，窗口 6 秒，稳定判定仍要求同一跟踪下的连续帧。引导计时使用独立 `GuidedEngine`，不推断阶段、次数、目标或时间指标；手动“记一次”记为人工标注，与测量分开保存；`measurement_mode='guided_timed'` 的记录不作为身体档案评估依据，纵向比较版本升为 `recorded-conditions-4`（旧记录按 `auto_observed` 解释）。多人、身份不明、断流、推理异常、保存失败、隐私暂停和双摄门禁一律不变，未放宽置信度、几何或计次规则，未新增模型、依赖、动作或示范素材。详见[0.17 说明](../docs/history/SMOOTH_FLOW_V0_17.md)与[本机验收](../docs/validation/SMOOTH_FLOW_V0_17_2026-09-12.md)。

2026-09-12 银发增量授权：按用户提供的 `SILVER_HEALTH_INCREMENTAL_PLAN.md`，0.16.0 复用既有双摄 / 姿态 / 会话，接入活动闭环、固定参考变化、本机共享回执与人工整改。额外允许手动开启的隔离 SYNTHETIC / TEST 手机前台演示，不包含患者库、真实求助或视频；不改变下文对正式远程部署、全屋并行、诊断和新网络训练的限制。原库不迁移，支持记录单独存储；可选操作走独立有界队列，禁止阻塞实时采集。实际范围、算法与未完成目标见[0.16 说明](../docs/history/SILVER_HEALTH_V0_16.md)。

2026-09-12 用户授权融合：当前 0.15.0 将本地 `199a265` 的逐步流程 / 结果解读与 GitHub `f7c69bb` 的提示降噪 / 倒计时 / 双摄逐项有效性合并。起点、方向、坐站步骤均走 `prepare_sample`；采样未结束不推进、不重复请求，保留取消 / 停止。最终“已核对，确认准备”是明确人工确认动作，不再叠加同义勾选框；陪同、训练安排和后台门禁保留，确认不会自动开始。普通步骤、动作图解与大字指导共享提示来源，报告保留双摄新旧有效性语义和详细证据。用户已反馈双摄可用；下文“第二相机尚未接入”为 0.13.0 开发时的历史记录。本轮软件验收不代替真人准确度验收，见[融合验收](../docs/validation/MERGE_V0_15_2026-09-12.md)。

2026-09-11 用户授权：0.14.0 将数据有效性与界面打断分离。普通 / 大字界面共享提示策略；无效帧立刻排除并清空当前读数，持续 1.5 秒缺测才给一条调整指令，身份歧义、断流、推理异常和保存失败不受缓冲。双摄采用 `primary-with-auxiliary-identity-1`：两路当前人员归属明确、主路必要点合格时，辅助几何缺测仅标记对应指标；缺人、身份不明或任一路输入 / 推理失败仍停用，异常停止真实任务，不只改变界面。报告保留逐项 valid/reason、实际主测量纳入标记；比较版本 `recorded-conditions-3` 区分旧双摄门槛。准备改用显式起点 / 方向按钮、可取消倒计时与连续稳定新帧，最后一次人工核对不自动开始；换动作、换人、机位或上下文变更后重新准备。详见 [0.14.0 说明](../docs/history/QUIET_GUIDANCE_V0_14.md)，未放宽置信度、几何或计次规则，未添加动作图片、模型或数据库迁移。

当前 0.13.0 双摄软件实现及验收见 `../docs/history/DUAL_CAMERA_V0_13.md`。纵向条件版本已增补为 `recorded-conditions-2`，逐路来源、模型、尺寸和配对条件均参与核对；未改写旧记录或数据库结构。软件回归通过不代表两台真实设备或真人准确度验收，第二相机尚未接入。

2026-09-10 双摄授权：用户明确要求“摄像头到了，做一下双摄实现”，覆盖下文历史单路数量限制。本次接入同一参与者的正面 / 侧面两个本地 RGB 摄像头，仍只有一个活动康复任务；由一个 CameraManager 统一拥有和释放两路 SourceWorker。采用接收时间配对、独立二维测量和逐路来源，不拼接跨视角坐标、不宣称曝光同步或三维角度。启动失败回滚两路、旧上下文拒收、任一路失效停止双摄任务、隐私暂停释放两路；保存失败门禁和原始数据授权仍适用。实施与实际验收见 `../docs/plans/DUAL_CAMERA_2026-09-10.md`。

2026-09-10 续建第三阶段：0.12.0 新增按原始快照逐项核对条件的纵向历史，比较版本 `recorded-conditions-1`。只在用户 / 来源 / 情境 / 动作 / 侧别 / 模式范围内排列记录；真实基线和计划参数必须精确一致，缺失双方不视为相同。排除名称、评估引用和确认时间等非测量元数据；不使用包含这些字段的全计划 / 机位版本哈希代替数值核对。失败和缺测保留并断开曲线；导出重查整个已查看快照的指纹，去标识化且不重写原记录。见 `../docs/history/LONGITUDINAL_HISTORY_V0_12.md`。

2026-09-10 续建第二阶段：0.11.0 在原计数之外新增出程、峰区 / 站位停留、回程和连续目标保持的观测时间，版本 `observed-timing-1`。关节阶段仅在完整往返结束后由连续三点中位数与单段峰区统计；实时保持只用已经到达的连续有效帧。坐站在站位计数，回程单列；缺测、换人、暂停不补计。可选时间安排由人工填写并保存，目标结果不改变完成次数；旧报告不补算。见 `../docs/history/MOVEMENT_TIMING_V0_11.md`。

2026-09-10 长期目标续建：用户要求继续推进未完成目标。0.10.0 新增本地个人多项目训练计划库，按用户 / 来源 / 情境隔离；复用不继承机位确认和基线，开始时重查计划版本、当前评估与测量定义。每次训练保存计划来源及临时改动的不可变快照，后续编辑和归档不重写旧报告。SQLite v4 升级前备份，保留只读旧库路径。见 `../docs/plans/PRODUCT_CONTINUATION_2026-09-10.md` 和 `../docs/history/TRAINING_PLAN_LIBRARY_V0_10.md`；单路采集和真人验收边界仍保留。

2026-09-10 整理：当前应用版本为 0.9.2，保留 53 项任务和已接入的单路可选关键点组件。本目录名称是历史实施包名称，不表示仍只有首版两项动作。文档已集中到 `../docs/`，当前状态与待办见 `../docs/HANDOFF.md`。下文按日期保留的增补解释功能演进；原始首周数量限制不能覆盖之后明确接入的功能。每次完成更新须按根目录约定提交、推送并核对 GitHub。

2026-09-10 增补：修复头颈侧位取景反馈和肩内收起点准备。颈前屈/后伸仍为同侧眼、耳、肩、髋的原二维定义，不要求远侧肩，不降低置信度或补点；提示指出具体缺失点。肩内收需人工确认侧抬臂起点，拒绝没有足够内收分期空间的起点；不改为水平内收，不自动增大动作范围。用户提出双摄方案，当前仅完成方案分析，未实现双路；后续明确接入时需单独更新采集、同步、保存与隐私契约。详见 `../docs/history/NECK_SHOULDER_FIX_V0_9_1.md`。

2026-09-09 最新增补：摄像头默认镜像，镜像只影响显示，不翻转原始图像/关节点语义或 ROI 存储。正式评估/训练支持自动进入大字指导，默认 36 像素、可选 32–48 像素，大图与当前阶段文字联动；数据不足/暂停/断流不得继续显示旧动作图或纠错。用户明确本轮仍不生成动作图片，保留真实图片接入和缺图标记。见 `../docs/history/DISTANCE_GUIDANCE_V0_9.md`，原模型、计数、确认、隐私和保存门禁不变。

2026-09-09 最新增补：首页提供独立“打开摄像头并测试”，用户点击才采集，原始画面不做姿态推理或生成评估记录；结束须确认释放，正式任务仍重新预览/校准/确认。默认动作目录改为人体部位导航，点击关节图标或名称显示对应动作。允许本次人体导航插图，仍不生成动作示范图、不增加精细测量功能；现有 53 项任务及数据安全门禁保留。详细契约见 `../docs/history/BODY_NAVIGATION_V0_8.md`。

2026-09-09 本轮增补：继续减少主界面的重复说明，突出动作、当前提示和下一步。摄像头选择全程序共享，启动恢复上次设备/接口；首次仅在排除明确标注的虚拟/IR/深度输入后剩余唯一有效候选时默认选中，不自动打开输入。不唯一、上次设备缺失或后端改变时人工重选；切模式仍须停止保存、释放并重新确认机位。实现见 `../docs/history/CLARITY_UI_V0_7.md`。

2026-09-09 最新用户调整：暂不继续实现当前技术条件不足的精细关节和体表标志点功能；现在优先调整现有桌面前端，参考 Keep 等运动 App 组织评估、训练和动作指导。提示图片本轮不生成、不下载，保留文字和明确图片位。实现与素材契约见 `../docs/history/SPORT_UI_V0_6.md`，此要求取代下方“暂不投入前端打磨”的旧优先级。保留既有可用动作及全部测量/数据安全边界。

2026-09-08 最新优先级：用户要求暂不投入前端打磨，界面可用即可，优先完善功能。只做功能所需入口与错误反馈；不继续视觉重设计。新增功能与仍未完成范围见 `../docs/history/FUNCTIONAL_EXPANSION_V0_5.md`。

2026-09-08 持续产品目标见 `../docs/plans/PRODUCT_COMPLETION_PLAN.md`：完善本地评估/档案/训练流程、技术可支持的关节动作，以及产品化原生 UI 和简洁文案。暂不做账号、隐私平台和远程部署；保留原有本地数据保护、单路采集和非诊断边界。阶段更新不等于此完整目标完成。

2026-09-08 第二次用户授权增补见 `../docs/history/JOINT_EXPANSION_V0_3.md`：扩展腕、踝和手指，允许必要的本地依赖和官方模型。当前为 33 项任务、左右侧独立；可选 MediaPipe 使用隔离环境、同一路输入，不替换原 YOLO 环境。下文首周冻结的动作/模型数量不限制这次明确授权；单路输入、数据保存、来源标签、隐私和非诊断边界仍须遵守。

2026-09-08 用户授权的应用范围增补见 `../docs/history/ASSESSMENT_TRAINING_V0_2.md`：rehab 评估/训练分为两个界面入口，新增用户身体信息汇总与四项关节动作。下文“两项核心动作”为原始首版基线，不限制本次已明确要求的六项动作；其他单路输入、隐私、保存与非诊断约束保持不变。

先读`../docs/specifications/SOFTWARE_SPEC.md`、本文件、`../docs/history/CHANGELOG.md`和`../docs/specifications/START_WITH_CODEX.md`。旧v1多摄像头/雷达首周要求已取消，v2.1只是稳定落地规则补充，不扩大硬件与模型范围。

## 当前范围
- 先使用笔记本内置RGB摄像头。外接普通UVC USB摄像头可以后到，不阻塞开发。
- 四场景：rehab / activity / bedroom_demo / safety_demo。所有场景可绑定一个设备；同时只允许一个活动场景、一路输入。
- 核心：两项康复动作（坐站、肩外展）的评估、训练指导、质量监督、保存和重开报告。
- 配套：可见久坐提醒、站立/步行任务；床边/低位的受控演示、规则风险提示、警报确认。
- 未启用场景=未监测。不得展示四房间同时正常，不得让用户误以为此时仍有全屋监护。
- RTSP、多流、雷达、跨房间、云端、手机App、三维/疾病诊断/模型训练均移至未来；不列为主依赖或启动检查。

## 输入与状态
- 只有CameraManager/它拥有的SourceWorker能开VideoCapture；禁止各业务类自行VideoCapture(0)。
- 用cv2-enumerate-cameras，显式backend枚举并用返回的index+backend打开。下拉框行号不等于设备index。
- 保存设备path/backend等线索，每次重开重新匹配；找不到/不唯一则人工选择，不静默fallback到0或别的设备。
- 刷新仅枚举；场景/源/动作/侧别/机位切换先结束并保存会话，确认旧采集已释放，再预览新输入。
- 临时状态全部清空，历史报告和未关闭警报保留。旧generation回调/画面/声音必须丢弃。
- 隐私暂停真正停止采集；恢复需确认，不给缺测时间补计。保存失败不得假称成功。
- 模型权重复用，跟踪器与规则在新的运行上下文重置；不把track_id当个人身份。
- 实际shape/FPS与请求参数分开，live用单调时间、replay用媒体时间；参数和后端能力需实测。
- ROI保存原图未镜像归一化坐标，UI点击先去留白、逆镜像；几何角用同尺度像素。
- 低质量返回null；未检测到人、未知、离线、隐私暂停、未启用分别显示。

## 业务与诚实性
- 坐站在达到站位计一次，回坐后才可再计；肩外展完整往返和达到目标分开。
- 个人目标人工设置，null仅测量；最多两条动作质量反馈；不用LLM实时开处方。
- 风险提示不是跌倒概率预测。警报不因断流或换模式自动关闭；已查看不等于救援已到场。
- LIVE_CAMERA / REPLAY_FILE / SYNTHETIC与SELF_USE / CONTROLLED_DEMO / TEST分别标记并持久化。
- 没有外接相机就记未实测，不用fake capture宣称USB热插拔通过。
- 优先安全受控动作、手动停止；禁止诱导老人/患者真实跌倒。
- 保留现有可运行代码与用户数据；先做迁移备份，不直接删旧数据库/文件。
- 模型/依赖来自可信来源，锁版本并记录许可；不破坏原科研环境。

## 本次补充的稳定性与资源约束
- 姿态→几何→阶段→质量→事件分层；不为每个动作找新模型，主依赖保持不变。
- Sports2D/Kinovea只是可选离线参考；STGCN-rehab、REHAB24-6、旧跌倒权重等不是本周运行依赖，禁止自动clone/下载/安装训练环境。
- 骨架schema、坐标系、关节顺序、模型hash显式记录；点数相同不表示兼容，缺点不复制填充。
- 保留原图中心/框/时间与局部归一化姿态；不要抹掉整体下降信息。实时判断不使用未来帧。
- 观察有效性、动作完成、个人目标达成、可见问题分开保存；允许多种问题并存。目标null不判未达标，缺测不判动作差。
- 逐项指标有valid/reason；仅对有证据的指标输出，不让一个无关点缺失把全部结果归零。
- 规则与目标运行中不悄悄更新，当前会话使用不可变快照；每次修改跑固定回归。
- 调试导出尊重逐帧骨架/视频授权；普通指标报告不依赖同意录像；预测与人工标签分开，NaN转null。
- 开发/最终验收按人及录制分组，先分组再切窗口，不以自己的预测为真值，不把回放或合成测试当实机。
- 新功能先补测试，新增验收要求尚未实现不得写成已通过。缺可选数据集不允许阻塞主界面。
- 许可从研究阶段就记录，代码/权重/数据分别核查，不以Demo名称规避限制。

## 交付方式
先完成选场景→选相机→预览→一项任务→保存→重开报告，再扩展剩余规则。
每阶段报告实际修改、运行命令、实际测试、未通过项和实机依赖。
本包只是任务书/参考代码/检查脚本，不是已有完整产品；不要声称继承了一个已完成UI。
